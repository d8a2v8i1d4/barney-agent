# 2026-09-13（日）

## 主題：兩個情報 routine 掛掉的排查與修復

### 問題 1：信用卡 / 機票日報連三天沒寄草稿（9/11–9/13）

**真因**：兩個 routine 設 `claude-fable-5-1`，該模型額度用完（`rate_limit: rejected`，重置 2026-10-01 08:00 台北），run 只活 8 秒就結束。同時段用 `claude-opus-4-7` 的投資早報完全正常 → **額度是分模型計的**。

**處理**：
- 兩個 routine 模型改 `claude-opus-4-7`，當天補跑成功（deal-hunter 13 筆、flight-hunter 9 筆，草稿已寄出）。
- 踩坑：RemoteTrigger 的 update 不是 deep merge，只送 model 會把整份 prompt（events）清空。有備份，已完整還原並逐字比對。

### 問題 2（排查時發現）：去重複歷史從沒進過 main

**真因**：第七步的 `git push origin HEAD` 把檔案推到 `claude/xxx` 分支再開 PR，從未合併。下次 run 從 main clone，讀不到歷史 → 去重複失效近 4 個月，每天 lastrun.txt 都寫「過濾 0 筆」。累積 273 個分支。

**處理**：
1. 從 273 個分支撈回各日期最新版，補進 main：deal-hunter 118 天（5/19 起）、flight-hunter 103 天（6/1 起）。
2. **流程搬進 repo**：`000_Agent/workflows/deal-hunter-daily.md`、`flight-hunter-daily.md`，照 `daily-invest.md` 慣例。routine prompt 只剩一句「讀這個檔案照做」。以後改流程 = 改檔案 + push，不再碰 API。
3. 新增第九步「推回 main」（fetch + rebase + `push origin HEAD:main`），並要求 push 失敗必須在回覆最後一行明講。
4. 三個 routine（deal-hunter、flight-hunter、投資早報）的失敗通知（email + push）全部打開——先前三個管道都是 false，所以才會靜靜掛三天。

### 待辦 / 觀察

- 明天（9/14）09:00 的兩份日報是第一次跑新架構，要確認：(a) 有讀到 workflow 檔案；(b) 第九步有 push 進 main；(c) 去重複開始生效（lastrun.txt 的「過濾 N 筆」應該不再是 0）。
- 投資日報 routine 的 Step 8-4 同樣是 `git push origin HEAD`，靠 GitHub Actions 補救才沒出事，兩套機制並存，值得擇日統一。
- 273 個舊 `claude/*` 分支還在，不影響運作，要清再說。
- 舊踩坑仍在：cloud 環境的 WebFetch 對 loyaltylobby、doctorofcredit、awardwallet、pointstalent 等站一律 EGRESS_BLOCKED，情報只能靠搜尋摘要，查證強度受限。
